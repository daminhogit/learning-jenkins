pipeline {
    agent any
    triggers {
    pollSCM('* * * * *')
    }

    stages {
        stage('New') {
            steps {
                echo 'Hello World Lets Go Now'
            }
        }
    }
}
